// ponytail: stub. Regenerar tras issue 02 con:
//   supabase gen types typescript --project-id <id> > src/types/database.ts
// Hasta entonces el cliente es tipado pero sin tablas. No inventar el schema a mano acá.
export type Database = Record<string, never>
